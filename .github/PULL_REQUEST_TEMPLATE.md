## Summary

<!-- What does this PR do, and why? -->

## Test plan

- [ ] `pnpm lint` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm test` passes (frontend)
- [ ] `cargo test --manifest-path src-tauri/Cargo.toml` passes (Rust host)
- [ ] `pytest -q` passes in `src-python/` (Python worker), if touched
- [ ] No unrelated formatting/reformatting churn
